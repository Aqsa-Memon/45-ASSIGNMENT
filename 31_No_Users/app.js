var userName = ['zeeshan', 'kashif', 'danish', 'umair', 'zaid'];
if (userName.length === 0) {
    console.log('we need to find some users!');
}
else {
    userName = [];
    console.log('All user names has been removed.' + userName.length);
}
